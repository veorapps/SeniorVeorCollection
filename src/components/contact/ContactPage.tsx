import Link from "next/link";
import { Clock3, Mail, MapPin, Phone } from "lucide-react";
import type { ContactPageData, SiteSettings } from "@/domain/models";
import { Container } from "@/components/ui/Container";
import { Media } from "@/components/ui/Media";
import { TrustBar } from "@/components/layout/TrustBar";
import { ContactForm } from "./ContactForm";

export function ContactPage({ data, settings }: { data: ContactPageData; settings: SiteSettings }) { return <main>{data.hero.enabled ? <section className="relative isolate overflow-hidden border-y border-brand-line bg-brand-sand"><Media asset={data.hero.image} className="absolute inset-0 h-full w-full object-cover object-[68%_center]" loading="eager" sizes="100vw" /><div className="absolute inset-0 bg-linear-to-r from-brand-ivory via-brand-ivory/80 to-transparent" /><Container className="relative flex min-h-[23rem] items-center py-12"><div className="max-w-xl"><p className="text-xs font-semibold tracking-[0.2em] text-brand-gold uppercase">{data.hero.eyebrow}</p><h1 className="mt-4 font-display text-5xl leading-[0.92] text-brand-ink sm:text-6xl">{data.hero.title}</h1><p className="mt-5 text-sm leading-6 text-brand-muted">{data.hero.description}</p><Link className="mt-7 inline-flex min-h-11 items-center bg-brand-teal px-5 text-xs font-semibold tracking-[0.08em] text-brand-ivory uppercase" href={data.hero.cta.href}>{data.hero.cta.label}</Link></div></Container></section> : null}<section className="py-[var(--sv-space-xl)]"><Container className="grid gap-8 lg:grid-cols-3"><div><h2 className="font-display text-3xl text-brand-ink">Nasıl Yardımcı Olabiliriz?</h2><div className="mt-5 divide-y divide-brand-line border-y border-brand-line">{data.helpItems.filter((item) => item.enabled).sort((a,b)=>a.order-b.order).map((item) => <div className="py-4" key={item.id}><h3 className="text-xs font-semibold tracking-[0.08em] text-brand-ink uppercase">{item.title}</h3><p className="mt-1 text-xs leading-5 text-brand-muted">{item.description}</p></div>)}</div></div><div><h2 className="font-display text-3xl text-brand-ink">İletişim Formu</h2><div className="mt-5"><ContactForm /></div></div><div><h2 className="font-display text-3xl text-brand-ink">İletişim Bilgileri</h2><div className="mt-5 space-y-5 text-sm text-brand-muted"><Info icon={Phone} label="Telefon" value={settings.contact.phone} /><Info icon={Mail} label="E-posta" value={settings.contact.email} /><Info icon={Clock3} label="Çalışma Saatleri" value={settings.contact.workingHours.join(" · ")} /><Info icon={MapPin} label="Ofis & Showroom" value={settings.contact.address} /></div></div></Container></section><section className="border-y border-brand-line bg-brand-sand"><Container className="grid min-h-60 place-items-center"><div className="border border-brand-line bg-brand-paper p-6 text-center"><MapPin aria-hidden="true" className="mx-auto size-8 text-brand-gold" /><h2 className="mt-3 font-display text-2xl text-brand-ink">Senior Veor Collection Showroom</h2><p className="mt-2 text-sm text-brand-muted">{settings.contact.address}</p></div></Container></section>{data.faq.enabled ? <section className="py-[var(--sv-space-xl)]"><Container className="max-w-4xl"><h2 className="text-center font-display text-4xl text-brand-ink">{data.faq.title}</h2><div className="mt-7 divide-y divide-brand-line border-y border-brand-line">{data.faq.items.filter((item)=>item.enabled).sort((a,b)=>a.order-b.order).map((item)=><details className="group py-4" key={item.id}><summary className="cursor-pointer list-none pr-8 text-sm font-medium text-brand-ink">{item.question}<span className="float-right text-lg text-brand-gold">+</span></summary><p className="pt-3 text-sm leading-6 text-brand-muted">{item.answer}</p></details>)}</div></Container></section> : null}{data.trustBar.enabled ? <TrustBar items={data.trustBar.items} /> : null}</main>; }
function Info({ icon: Icon, label, value }: { icon: typeof Phone; label: string; value: string }) { return <div className="flex gap-3"><Icon aria-hidden="true" className="size-5 shrink-0 text-brand-gold" strokeWidth={1.4} /><p><strong className="block text-xs tracking-[0.08em] text-brand-ink uppercase">{label}</strong><span className="mt-1 block leading-6">{value}</span></p></div>; }
