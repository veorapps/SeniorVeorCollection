import Link from "next/link";
import { Clock3 } from "lucide-react";
import type { BlogPost } from "@/domain/models";
import { Media } from "@/components/ui/Media";

export function ArticleCard({ featured = false, post }: { featured?: boolean; post: BlogPost }) {
  return <article className={featured ? "grid overflow-hidden border border-brand-line bg-brand-paper md:grid-cols-[1.3fr_1fr]" : "overflow-hidden border border-brand-line bg-brand-paper"}><Link className="block overflow-hidden" href={`/blog/${post.slug}`}><Media asset={post.coverImage} className={featured ? "h-full min-h-52 object-cover transition-transform duration-300 hover:scale-[1.03]" : "aspect-[16/9] object-cover transition-transform duration-300 hover:scale-[1.03]"} sizes={featured ? "(min-width: 768px) 60vw, 100vw" : "(min-width: 1024px) 25vw, 50vw"} /></Link><div className={featured ? "flex flex-col justify-center p-5 lg:p-7" : "p-3.5"}><p className="text-[0.625rem] font-semibold tracking-[0.12em] text-brand-gold uppercase">Öne Çıkan Yazı</p><Link className="mt-2 block" href={`/blog/${post.slug}`}><h2 className={featured ? "font-display text-3xl leading-none text-brand-ink" : "font-display text-[1.4rem] leading-tight text-brand-ink"}>{post.title}</h2></Link><p className="mt-2 text-[0.75rem] leading-5 text-brand-muted">{post.excerpt}</p><div className="mt-3 flex items-center gap-2 text-[0.625rem] text-brand-muted"><Clock3 aria-hidden="true" className="size-3.5 text-brand-gold" strokeWidth={1.4} />{post.readingTimeMinutes} dk okuma</div><Link className="mt-3 inline-flex border-b border-brand-gold pb-1 text-[0.625rem] font-semibold tracking-[0.08em] text-brand-teal uppercase" href={`/blog/${post.slug}`}>Yazıyı Oku</Link></div></article>;
}
