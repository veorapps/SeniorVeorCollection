import Link from "next/link";
import { BadgeCheck, Clock3, HeartHandshake, ShieldCheck, Sparkles } from "lucide-react";
import type { AboutPageData, BenefitItem } from "@/domain/models";
import { Container } from "@/components/ui/Container";
import { Media } from "@/components/ui/Media";
import { SectionHeading } from "@/components/ui/SectionHeading";
import { TrustBar } from "@/components/layout/TrustBar";
import { NewsletterBanner } from "@/components/layout/NewsletterBanner";

const iconMap = { "badge-check": BadgeCheck, "clock-3": Clock3, "heart-handshake": HeartHandshake, "shield-check": ShieldCheck, sparkles: Sparkles };

export function AboutPage({ data }: { data: AboutPageData }) {
  return <main>{data.hero.enabled ? <section className="relative isolate overflow-hidden border-y border-brand-line bg-brand-sand"><Media asset={data.hero.image} className="absolute inset-0 h-full w-full object-cover object-[68%_center]" loading="eager" sizes="100vw" /><div className="absolute inset-0 bg-linear-to-r from-brand-ivory via-brand-ivory/80 to-transparent" /><Container className="relative flex min-h-[25rem] items-center py-12 lg:min-h-[28rem]"><div className="max-w-2xl"><p className="text-xs font-semibold tracking-[0.2em] text-brand-gold uppercase">{data.hero.eyebrow}</p><h1 className="mt-4 font-display text-5xl leading-[0.92] text-brand-ink sm:text-6xl lg:text-7xl">{data.hero.title}</h1><p className="mt-5 max-w-lg text-sm leading-6 text-brand-muted">{data.hero.description}</p><Link className="mt-7 inline-flex min-h-11 items-center bg-brand-teal px-5 text-xs font-semibold tracking-[0.08em] text-brand-ivory uppercase" href={data.hero.cta.href}>{data.hero.cta.label}</Link></div></Container></section> : null}{data.trustBar.enabled ? <TrustBar items={data.trustBar.items} /> : null}{data.story.enabled || data.craft.enabled ? <section className="py-[var(--sv-space-2xl)]"><Container className="grid gap-10 lg:grid-cols-[0.9fr_1fr_1fr] lg:items-center">{data.story.enabled ? <Media asset={data.story.image} className="aspect-[4/5] border border-brand-line object-cover" sizes="(min-width: 1024px) 30vw, 100vw" /> : null}{data.story.enabled ? <div><SectionHeading eyebrow={data.story.eyebrow} title={data.story.title} />{data.story.paragraphs.map((paragraph) => <p className="mt-4 text-sm leading-6 text-brand-muted" key={paragraph}>{paragraph}</p>)}</div> : null}{data.craft.enabled ? <div className="border-l border-brand-line pl-0 lg:pl-8"><SectionHeading description={data.craft.description} eyebrow={data.craft.eyebrow} title={data.craft.title} /><BenefitList className="mt-7" items={data.craft.benefits} /></div> : null}</Container></section> : null}{data.values.enabled ? <section className="border-y border-brand-line bg-brand-paper py-[var(--sv-space-xl)]"><Container><SectionHeading align="center" title={data.values.title} /><div className="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><BenefitCards items={data.values.items} /></div></Container></section> : null}{data.journey.enabled ? <section className="py-[var(--sv-space-xl)]"><Container><SectionHeading align="center" title={data.journey.title} /><ol className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-6">{data.journey.items.filter((item) => item.enabled).sort((a, b) => a.order - b.order).map((item, index) => <li className="relative border-t border-brand-gold pt-5" key={item.id}><span className="grid size-9 place-items-center rounded-full border border-brand-gold bg-brand-paper text-xs text-brand-gold">{String(index + 1).padStart(2, "0")}</span><h3 className="mt-4 text-xs font-semibold tracking-[0.08em] text-brand-ink uppercase">{item.title}</h3><p className="mt-2 text-xs leading-5 text-brand-muted">{item.description}</p></li>)}</ol></Container></section> : null}{data.packaging.enabled ? <section className="border-y border-brand-line bg-brand-sand"><Container className="grid lg:grid-cols-2"><div className="py-10 lg:py-16 lg:pr-10"><SectionHeading description={data.packaging.description} eyebrow={data.packaging.eyebrow} title={data.packaging.title} /><Link className="mt-7 inline-flex min-h-11 items-center bg-brand-teal px-5 text-xs font-semibold tracking-[0.08em] text-brand-ivory uppercase" href={data.packaging.cta.href}>{data.packaging.cta.label}</Link></div><Media asset={data.packaging.image} className="min-h-72 h-full object-cover" sizes="(min-width: 1024px) 50vw, 100vw" /></Container></section> : null}<NewsletterBanner description="Özel kampanyalar, yeni koleksiyonlar ve parfüm ipuçları e-posta kutunuzda." title="Kokunun Zarafetini Keşfedin" /></main>;
}

function BenefitList({ className, items }: { className?: string; items: BenefitItem[] }) { return <div className={className}>{items.filter((item) => item.enabled).sort((a, b) => a.order - b.order).map((item) => { const Icon = iconMap[item.icon as keyof typeof iconMap] ?? ShieldCheck; return <div className="flex gap-3 border-b border-brand-line py-4" key={item.id}><Icon aria-hidden="true" className="size-5 shrink-0 text-brand-gold" strokeWidth={1.35} /><div><h3 className="text-xs font-semibold tracking-[0.08em] text-brand-ink uppercase">{item.title}</h3><p className="mt-1 text-xs leading-5 text-brand-muted">{item.description}</p></div></div>; })}</div>; }

function BenefitCards({ items }: { items: BenefitItem[] }) { return <>{items.filter((item) => item.enabled).sort((a, b) => a.order - b.order).map((item) => { const Icon = iconMap[item.icon as keyof typeof iconMap] ?? ShieldCheck; return <article className="border border-brand-line bg-brand-ivory p-5 text-center" key={item.id}><Icon aria-hidden="true" className="mx-auto size-7 text-brand-gold" strokeWidth={1.3} /><h3 className="mt-4 font-display text-2xl text-brand-ink">{item.title}</h3><p className="mt-2 text-xs leading-5 text-brand-muted">{item.description}</p></article>; })}</>; }
